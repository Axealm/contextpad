# Local development only. Never put these values in apps/web or Git.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GMAIL_REDIRECT_URI=http://127.0.0.1:8000/api/v1/gmail/callback
APP_WEB_ORIGIN=http://127.0.0.1:5173
# Relative paths are resolved from apps/api, never from the shell directory.
CONTEXTPAD_DB_PATH=data/contextpad.sqlite3
